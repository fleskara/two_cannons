Za pokretanje skripte neophodno je instalirati naredne biblioteke:
- Matplotlib : `python -m pip install -U matplotlib`
- NumPy: `pip install numpy`  

Program se pokreće komandom `python3 two_cannons.py`
